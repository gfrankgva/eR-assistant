# Architecture Overview – eR-Assistant (Connected Mode)

## 1. Purpose
eR-Assistant runs inside the BPA modeling environment to validate services in real time.  
It merges *backend truth* (from BPA REST APIs) with *frontend state* (DOM data) and checks both against backend validation rules.

## 2. High-Level Diagram
[diagram omitted for brevity in file]
